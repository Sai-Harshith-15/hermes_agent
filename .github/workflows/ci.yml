# name: CI

# on:
#   push:
#     branches: ["main"]
#   pull_request:
#     branches: ["main"]

# jobs:
#   backend-test:
#     runs-on: ubuntu-latest
#     env:
#       SECRET_KEY: "ci_test_secret_key_not_for_production"
#       MASTER_KEY: "ZGVmYXVsdF9tYXN0ZXJfa2V5X2NoYW5nZV9tZV9ub3c="
#       HERMES_DIR: "/tmp/hermes"
#       ACCESS_TOKEN_EXPIRE_MINUTES: "30"
#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"

#       - name: Cache pip
#         uses: actions/cache@v4
#         with:
#           path: ~/.cache/pip
#           key: ${{ runner.os }}-pip-${{ hashFiles('backend/requirements.txt') }}
#           restore-keys: ${{ runner.os }}-pip-

#       - name: Install dependencies
#         run: |
#           cd backend
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt

#       - name: Run Pytest
#         run: |
#           cd backend
#           pytest --tb=short -q

#   frontend-check:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "20"

#       - name: Cache node_modules
#         uses: actions/cache@v4
#         with:
#           path: frontend/node_modules
#           key: ${{ runner.os }}-node-${{ hashFiles('frontend/package-lock.json') }}
#           restore-keys: ${{ runner.os }}-node-

#       - name: Install dependencies
#         run: |
#           cd frontend
#           npm ci

#       - name: TypeScript type check
#         run: |
#           cd frontend
#           npx tsc --noEmit

#       - name: Build (catch bundler errors)
#         run: |
#           cd frontend
#           npm run build
#         env:
#           VITE_API_BASE_URL: /api/v1
#           VITE_WS_BASE_URL: ws://localhost:8000/ws/telemetry
#           VITE_PTY_WS_URL: ws://localhost:8000/api/pty
